# Optional for a deployed frontend. Local development uses Vite /api -> localhost:8000.
# VITE_API_BASE_URL=https://your-backend.example.com
# This is a public backend URL, never an API secret. Rebuild after changing it.
